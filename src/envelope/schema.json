{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "vinv-Envelope",
    "description": "The envelope provides a structure for all vinv data.",
    "type": "object",
    "additionalProperties": false,
    "required": ["$schema"],
    "properties":{
        "$schema": {
            "type": "string",
            "format": "uri-reference",
            "const": "https://schema.vinv.io/envelope/0.0.1.schema.json",
            "examples": [
                "https://schema.vinv.io/envelope/0.0.1.schema.json"
            ]
        },
        "data": {
            "tite": "Data",
            "description": "The data to be validated.",
            "type": "object",
            "additionalProperties": false,
            "properties": {
                "vinv-individual-tree" : {
                    "type": "object"
                }
            }
        }
    }
}